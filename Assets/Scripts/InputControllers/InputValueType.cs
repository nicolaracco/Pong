namespace Pong.InputControllers
{
    /*
     * Destination value type is used when a touch or mousebutton is pressed
     * Delta value type is used for keyboard
     */
    public enum InputValueType { Destination, Delta }
}